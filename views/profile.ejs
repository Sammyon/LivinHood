<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
</head>
<%- include('./partials/navbar') %>
<body>
  </html>
  <hr>
  <h1>Profile</h1>
  <hr>
  <p>Name : <%= data.name %></p>
  <p>Email : <%= data.email %></p>
  <p>Role : <%= (data.isAdmin) ? 'Admin' : 'User' %></p>
  <p>Credit : <%= currency(data.credit) %></p>
  <p>Bank Account Number : <%= data.bankAccountNumber %></p>
  <p>Joined :<%=data.localeDate(data.createdAt) %></p>

  <a href="/portfolio"><button>Your Invesments</button></a>
  
</body>